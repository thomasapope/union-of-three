///sc_set_jump(h, w, target);

var height = argument[0];
var width = argument[1];
var target = argument[2];

target.h = height;
target.w = width;
//target.grav = (2 * height * sqr(RUN_SPEED)) / sqr(width);

//var val = argument[0];
//var target = argument[1];

//target.grav = val;
//return val;
