///sc_wall_jump(distance, axis)
var distance = argument[0];
var axis = argument[1];
var h_boost = distance + 3;

sc_jump_new(10, .52, distance, -h_boost, axis, self);
jy = y;
jump = 1;
acc = .2;
alarm[1] = 5;
if (air_jumps < MAX_JUMPS -1)
    air_jumps += 2;
