// Get Player's Input
key_right = keyboard_check(ord('D'));
//key_right_alt = keyboard_check(vk_right);

key_left = keyboard_check(ord('A'));
//key_left_alt = keyboard_check(vk_left);

key_jump_press = keyboard_check_pressed(ord('W'));
key_jump_release = keyboard_check_released(ord('W'));
//key_jump_press_alt = keyboard_check_pressed(vk_up);
//key_jump_release_alt = keyboard_check_released(vk_up);

key_down = keyboard_check(ord('S'));
//key_down_alt = keyboard_check(vk_down);

key_debug = keyboard_check_pressed(ord('G'))
