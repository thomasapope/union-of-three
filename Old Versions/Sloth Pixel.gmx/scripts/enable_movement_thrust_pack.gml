///enable_movement_thrust_pack(key pressed, key release, key)
/*
    Similar to air jumpng, this script allows
    for vertical movements in air, but thrust 
    packing happens over time instead of all 
    at once 
*/

var press = argument[0];
var release = argument[1];
var hold = argument[2];

// Calculate Air Jump Height

// Thrust Pack

/*if !ground && air_jumps > 0 && press {
    while thrust > 0 {
        vsp[0] -= .01;
        thrust -= .1;
    }
    air_jumps--;
}*/

/*if ground {
    thrust_reset();
}*/
