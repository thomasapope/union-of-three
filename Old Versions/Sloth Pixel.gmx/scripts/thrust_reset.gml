/// thrust_reset()
// resets thrust to max_thrust
thrust = max_thrust;

/*var i;
        for (i = 0; i < instance_number(system); i += 1)
            {
                control[i] = instance_find(system,i);
            }
        if control[i].channel = channel {
            if control[i].powered = 0
                control[i].powered = 1;
            if control[i].powered = 1
                control[i].powered = 0;
        }*/
