/// @description  Initialize
target_room = room;
target_x = x + (sprite_width / 2);
target_y = y + (sprite_height / 2);

border_door = false;
border_dir = 0;

// Fade settings
FADE_DISTANCE = 48;
fade = noone;

xx = x + sprite_width / 2;

