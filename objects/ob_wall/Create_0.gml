// Check if wall is a ledge
lledge = 0;
rledge = 0;

glare_w = 4;
glare_l = 32;
yy = y + (glare_w / 4);

/*// Check Collision on left
if (!place_meeting(bbox_left, y, PAR_SOLID) 
    && !place_meeting(bbox_left+1, bbox_top-1, PAR_SOLID)) {
    lledge = 1;
}
// Check Collision on right
if (!place_meeting(bbox_right, y, PAR_SOLID) 
    && !place_meeting(bbox_right-1, bbox_top-1, PAR_SOLID)) {
    rledge = 1;
}*/

/* */
/*  */
